{
  "name": "bypostamaker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix worker\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "deploy": "npm run build && npm run deploy:production --prefix worker",
    "deploy:staging": "npm run build && npm run deploy:staging --prefix worker",
    "db:migrate": "npm run db:migrate:prod --prefix worker",
    "type-check": "concurrently \"npm run type-check --prefix worker\" \"npm run type-check --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@ai-sdk/google": "^3.0.83",
    "@ai-sdk/groq": "^3.0.42",
    "@ailink/sdk": "^0.3.0",
    "@cf-wasm/photon": "^0.3.6",
    "ai": "^6.0.208",
    "jszip": "^3.10.1"
  }
}
